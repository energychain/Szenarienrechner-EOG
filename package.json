{
  "name": "regulierte-sparten-szenario-rechner",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "build:story": "node scripts/build-story.mjs",
    "build:docs": "node scripts/build-docs.mjs",
    "build:manifest": "node scripts/build-release-manifest.mjs",
    "build:release": "npm run build && npm run build:story && npm run build:docs && cp llm.txt llms.txt dist/ && cp dist/index.html dist/szenarienrechner-eog.html && npm run build:manifest",
    "lint": "eslint src tests scripts",
    "test": "vitest run",
    "test:distribution": "node scripts/check-distribution.mjs",
    "test:docs": "node scripts/check-docs.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@eslint/js": "^9.31.0",
    "@types/node": "^26.1.1",
    "eslint": "^9.31.0",
    "globals": "^15.15.0",
    "typescript": "^5.8.3",
    "vite": "^7.0.4",
    "vite-plugin-singlefile": "^2.3.0",
    "vitest": "^3.2.4"
  }
}
